# Server
PORT=3000

# Sync-Intervall fuer iCal-Abgleich (Cron-Syntax). Standard: alle 15 Minuten
SYNC_CRON="*/15 * * * *"

# Optional: Telegram-Benachrichtigung an den Admin
# Bot bei @BotFather erstellen, Token hier eintragen.
# Chat-ID bekommst du z.B. ueber https://api.telegram.org/bot<TOKEN>/getUpdates
# nachdem du dem Bot einmal geschrieben hast.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
